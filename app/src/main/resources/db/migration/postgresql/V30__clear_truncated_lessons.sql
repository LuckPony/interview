-- ============ 清空被误截断的子知识点讲解缓存 ============
--
-- 背景：V19 引入的 concept_lesson 缓存，在旧版 LessonGenerator 的「重启检测」逻辑下，
-- 会把正文里出现的第二个「1.」编号列表误判为「讲解收尾后从头再讲」，把讲解拦腰截断
-- （只显示一半）；另外 5000 字符硬上限也会截断超长讲解。这些被截断的文本已写入缓存，
-- 修复逻辑无法自动复原，必须清掉让讲解按修复后的逻辑重新生成（打开即懒生成，一次性成本）。
DELETE FROM concept_lesson;
