# 面霸 · Release Notes

## v0.4.0

### 🐛 修复
- **修复练习页白屏**：安装包内嵌旧后端缺少 `subPoints` 字段导致点击知识点后整页崩溃；已重建后端 jar 并给前端 `Plans.tsx` 加字段容错（`?? []`），即使后端缺字段也不白屏。
- **修复 Flyway 迁移撞号**：`postgresql/V30__clear_truncated_lessons.sql` 与 `common/V30__interview_question.sql` 同版本号会导致后端启动失败；已重命名为 `V35`（高于库内已应用的 V34），服务器部署同样受益。
- **修复学习计划排序漂移**：进入知识点（会写回子知识点拆解缓存）后返回学习计划，知识点排列顺序不再变化——服务端改为按「层级 + id」确定性排序，不再依赖数据库无序返回。

### 🧭 导航
- 知识点页与做题页新增「返回」按钮（保留「换个方向」），支持**鼠标后退键**逐级返回：做题 → 子知识点清单 → 学习计划首页；返回时自动取消流式请求并清理会话。

### 🎯 出题范围（三级）
- **子知识点出题**：范围 = 已学内容 + 该子知识点（原有）。
- **按整个知识点出题**（知识点页新按钮）：范围 = 已学内容 + 整个知识点，综合多个子知识点考察。
- **按 L{n} 整个层级出题**（知识点页 + 学习计划页「练这一层」）：范围 = 已学内容 + 整个层级，跨知识点建立工程联系；题面参与概念数受出题类型上限约束，但上下文注入整层全部概念。

### 🚀 发布
- 新增云模式一键发布脚本 `interview-desktop/scripts/release.sh`（`MIANBA_SERVER + GH_TOKEN npm run release`），推送到 `main` 后 GitHub Actions 自动构建云模式安装包并发布 GitHub Release。

### 📋 数据库迁移
- `V35__clear_truncated_lessons.sql`：清空被误截断的子知识点讲解缓存（原 V30 重命名避让）。

---

## v0.7（本期，未发版号）

### ✨ 子知识点「直接通过」
- 讲解页新增「直接通过 / 取消通过」按钮：简单知识点不用做题，点击后确认弹窗「确定跳过此子知识点吗？」，标记为已通过（清单页 ✓）；可随时取消通过恢复为待学习
- 后端新增 `sub_point_pass` 表（V36）+ `POST /drill/{conceptId}/sub-point-pass`；学习计划、统一工作流、知识点清单三处「已达标子点」均并入手动通过记录

### 🧩 子知识点拆解优化 + 用户自管
- **不再强拆 7/8 个**：拆解提示词改为「数量由知识点内容决定（少则 2-3 个，内容丰富可多拆、无上限），绝不凑数」，并明确要求各子点内容明显不同、互不重叠
- **语义去重兜底**：拆解结果按字符二元组相似度 + 包含关系过滤近似重复（如「列表推导式」与「列表推导式的用法」只保留一个），防止模型换说法凑数
- **用户可增删子知识点**：清单页新增「新增子知识点」按钮（输入名称即加入，讲解首次打开时按需生成）；每项悬停显示删除按钮（确认后删除，讲解缓存与「通过」记录一并清理）

### 💬 对话输入与暂停
- 输入框**回车 = 换行**（写代码不再误发），**Ctrl/⌘+回车 = 发送**，Tab 插入缩进；输入框改等宽字体、关闭自动纠错
- AI 回复流式期间显示「暂停」按钮：暂停后已显示内容保留在界面（带「已暂停」标记），**未答完的回复不写入数据库**（服务端检测客户端断开后跳过落库），可继续提问

### 🤖 对话体验
- 明确禁止 AI 要求学生复述/重复/转述题目、自己的回答或讲解内容（练习对话两处提示词），AI 必须基于已有对话直接推进

### 📊 评分基准修正
- 判分只依据学生在「获得完整答案之前」独立给出的回答：对话实录中老师一旦已给出完整答案/完整代码/把关键思路讲到位（含「看答案」揭示），其后的学生回答一律判 NA 不计分——引导后照抄答案不再得分

### 🧪 测试
- 修复预置失败：`ResumeRepository` 补充 `findByContentHash`（样例测试可编译），全量 drill 模块单测通过

---

## v0.6（本期，未发版号）

### 🧾 对话沉淀（随手记）
- 新增「对话沉淀」页面（/capture）：日常自由问答走 SSE 逐 token 流式，回答遵循知识问答助手提示词（结论→原理→示例→易错点，Markdown 结构，**禁止输出 question:/answer: 等字段前缀**）
- 空状态引导：无对话时显示示例问题，**按已有卡片标签出现频次**动态推荐 top4（无标签时回退默认示例）
- 思考中占位：AI 未出字前显示「思考中…」跳动动画 + 光标，替代空白等待
- 自由问答后端改用 `LlmRawClient` 原生直连：按请求解析 LLM key（请求头 X-LLM-Key > 用户设置 > 启动配置），修复注册中心无服务器级 key 时提问必败的问题；前端 SSE 请求同步补带 `X-LLM-Key` 头

### 🃏 知识卡片
- 新增 `knowledge_card` 表（V20）+ `detail` 列（V21）：question / answer（AI 提炼摘要）/ detail（AI 当时完整回复 Markdown 原文）/ tags（逗号分隔）/ concept_id（可选关联概念）/ 复习排程字段
- `ChatCaptureService` 一键成卡：LLM 结构化提炼 question/answer/tags + 关联已有概念（LLM 判断不相关则不关联）；无价值对话（空 question）不落库并明确提示；自动清理模型输出的字段前缀
- 卡片列表：标签筛选 chips（带计数）、标签名/问题内容模糊搜索、本地分页（每页 8 张）、标签 Badge 美化（# 前缀 + hover 动效）
- 标签栏折叠：卡片标签与筛选标签栏均**最多两行**，溢出显示带箭头的「展开」/「收起」（动态测量，不溢出不显示）
- 卡片编辑弹窗：问题 / 答案摘要 / 详细答案 / 标签可编辑，保存后同步内化复盘（同一张表）

### 🔍 两级查看答案
- 「查看答案」→ 显示 AI 总结摘要；「查看详细答案」→ 还原 AI 当时完整回复（Markdown 渲染，靛蓝底标注「AI 完整回答」）
- 对话沉淀与内化复盘交互统一；旧卡片无 detail 时给出提示

### 🔗 卡片 ↔ 知识点同步
- 新卡创建**当天到期**（创建即到期，保证当天进内化复盘；针对 Hibernate UTC 时区做了 30 秒回退保证立即可见）
- 掌握后按 FSRS 间隔 1/3/7/15/30 天排程；没掌握明天重来
- 复习反馈按概念粒度回写 `mastery`：掌握 +1（封顶 L2，L3 仅模拟面试达标）、没掌握 -1（不封底 0）

### 📝 内化复盘
- 顶部下拉框切换「学习中（练习欠账）/ 知识卡片（到期复习）」
- 知识卡片复习前只显示标题（不显示标签），点「查看答案」展开摘要、「查看详细答案」展开完整记录；「没掌握 / 掌握了」反馈并同步概念掌握度
- 知识卡片样式重做（review-card：白卡 + 靛蓝标题竖线 + 内容区分层）

### 🔐 安全
- `/api/knowledge/**` 纳入 JWT 鉴权链（防止未登录/公网裸奔调用 LLM）

### 📋 数据库迁移
- `V20__knowledge_card.sql`：新增 `knowledge_card` 表
- `V21__knowledge_card_detail.sql`：新增 `detail` 列（AI 完整回复）

### ⚠️ 时区注意
- 后端 JVM 时区为 UTC 时，`timestamp`（无时区）列与 PG 默认值存在 8 小时表示差异；新卡 due_at 已做 30 秒回退，存量卡片如需当天到期需人工修正 due_at（本次已修正）

---

## v0.3.26

- 修复普通回答被误判为“看答案”的问题：答案揭示现在只能由用户点击「看答案」显式触发。
- 取消 AI 自动结束会话与自动评分；AI 停止追问后会提示用户自行点击「结束并评分」。
- 收紧评分范围，只评估题目正文明确要求的内容，未提出的候选追问和扩展知识不再扣分或显示在评分标准中。
- 练习首页按账号展示所有学习方向的待学习、待复习任务，并补充知识点、层级和子知识点达标状态。
- 设置页改为响应式多卡片布局；学习提醒升级为每天或每周多选日期，并采用 Ant Design 时间、周期和开关组件。
- 默认继续使用本地 H2 文件数据库，并支持通过 `APP_DATASOURCE_*` 环境变量切换数据源。
- Flyway 学习工作流迁移调整为 V23、V24，避免与知识卡片 V21 迁移冲突。
- 前端版本升级至 v0.3.8，桌面应用升级至 v0.3.26。

## v0.3.25

- 新增 L1 到 L5 的统一学习工作流：按子知识点教学与练习、大知识点综合检测、层级综合检测顺序推进，同时允许从完整学习计划自由进入任意知识点。
- 子知识点重复练习会参考用户此前的题目、回答与辅导对话生成不同场景的新题，并增加相似题二次生成保护。
- 优化辅导对话：正确回答不再重复询问“是否理解”，有价值时直接追问，否则自动结束并评分；查看答案后也按揭示前回答自动评分。
- 练习首页明确展示今日待学习与待复习任务；桌面端新增每日提醒时间、托盘驻留系统通知和测试通知。
- 完善子知识点达标进度、大知识点与层级综合检测证据、学习计划全量知识地图及响应式聊天布局。
- 前端版本升级至 v0.3.7，桌面应用升级至 v0.3.25。

## v0.3.24

- 子知识点完成状态独立记录：完成某个子知识点后只给该子点打勾，不再把整个大知识点显示完成；再次进入时可恢复已完成子点。
- AI 出题与讲解优先提供最小可运行代码/配置；调用链、生命周期、架构和状态流转可输出 Mermaid 示意图，前端直接渲染。
- 全局应用内容区改为可用空间的 80%，所有页面统一；练习聊天面板和消息气泡跟随父容器宽度，不再固定为窄列。
- 前端版本升级至 v0.3.6，桌面应用升级至 v0.3.24。

## v0.5（本期，未发版号）

### 🧠 学习上下文注入层（核心）
- 新增 `ProgressContextService`：每次 AI 调用统一注入 **学生画像（掌握度/最近判分/复习到期/层掌握率）+ 概念要点 + 资料块 + 互联网补充**，出题 / 对话 / 判分 / 复盘四处接入
- **出题循序渐进**：画像注入出题 prompt，掌握度低从基础定义/最小示例开始，掌握度高才出进阶/综合题
- **破除信息茧房**：资料与互联网均为「素材」而非天花板，prompt 明确「资料没覆盖的概念用通用知识照常考，但不得把通用知识冒充成资料内容」
- **判分可审计**：判分只依据已注入上下文与用户实际回答，禁止判分时额外联网搜索

### 📚 资料结构化（不截断语义）
- 新增 `CorpusIndexer`：资料按**逻辑主题切块**（服务端启发式切边界、LLM 只标注标题/知识点/摘要，原文零改动零丢失；不硬切字数，仅保护上限 20k/块、30 块）
- 新增 `corpus_chunk` / `concept_chunk` 表：概念 ↔ 资料块映射，出题/对话按概念**检索命中块**而非全文截断
- **知识点确认 UI**：上传资料后异步提取候选知识点，建计划页展示，可勾选「并入规划」
- 索引未完成时优雅回退为整篇资料注入，不阻塞建计划

### 🌐 互联网增强
- `LlmRawClient` 新增 `webSearch`：Responses API 内置 web_search 工具优先，chat/completions 方式回退（供应商不支持时自动降级，不报错）
- 建计划默认对每个知识点预取一次互联网标准内容存 `web_content`（无开关，异步执行，失败跳过）

### 💬 追问与答案
- 题干只抛一个主问；对话中**确认学生理解当前小问后才出下一问**（≤4 小问，后端安全阀兜底）
- **看答案按进度**：reveal 时 AI 定位「当前小问」只讲它的答案，不再一次倒出全部；揭示后不再追问
- 答案揭示边界（V14）保留：评分只取「得到答案之前」的回答

### 🗑️ 记录管理（此前版本）
- 删除学习计划 / 问答记录 / 内化复盘，均带二次确认 + 不可恢复提示

### 🔐 安全（此前版本）
- 移除服务器级/共享 API Key：key 只来自用户设置或桌面端 `X-LLM-Key` 头（只用不存），后端只返回 `hasApiKey`
- Flyway 乱序容错仅限开发环境

### 🖥️ 桌面应用
- Windows 与 macOS 桌面应用内嵌精简 JRE、Spring Boot fat jar 和静态 SPA，安装后无需另行配置 Java、Docker 或数据库
- 本地模式自动启动内嵌后端，用户数据和 H2 数据库保存在 Electron 用户数据目录
- 支持本地文件与目录读取，可将本机资料直接用于学习流程
- 关闭主窗口后驻留系统托盘，应用和本地后端继续运行，为后台提醒和定时任务保留运行基础
- 托盘支持恢复窗口和“彻底退出”；彻底退出时会清理整个 Java 后端进程树
- Windows 可执行文件、任务栏、快捷方式和托盘统一使用面霸图标
- 安装版通过 GitHub Releases 检查更新：发现更新时立即提示、后台下载并显示托盘进度，下载完成后可重启安装
- 更新请求使用系统代理，并仅让 `localhost` / `127.0.0.1` 绕过代理；更新过程记录到用户目录的 `updater.log`
- GitHub Actions 分别构建 Windows NSIS/ZIP 与 macOS DMG/ZIP，并统一发布安装包、blockmap 和 `latest*.yml`
- macOS 未使用有效 Developer ID 签名和公证时，首次启动与自动更新仍可能受到 Gatekeeper 限制

### 📋 数据库迁移
- `V16__corpus_chunk_and_web_content.sql`：新增 `corpus_chunk` / `concept_chunk` / `web_content` 三表（FK 带 CASCADE）

### 🚀 部署说明
1. 服务器：`deploy.sh`（git pull → 重编译 → 重启）生效 V16 迁移
2. 前端改动随后端一起部署
3. 桌面应用：推送到 `main` 后由 GitHub Actions 构建 Windows/macOS 安装包并发布到 GitHub Releases；也可在 macOS 中执行 `npm --prefix interview-desktop run release:local` 本地发布
4. 清理服务器 `/workspace/.env` 中已失效的 `API_KEY=`（可选）

### ⚠️ 已知限制
- 联网搜索依赖供应商 API 支持（DeepSeek 官方走 Responses API 的 web_search 工具）；不支持时自动降级为「资料 + 通用知识」出题
- macOS 自动更新需 Apple 开发者签名（$99/年）；未签名只能手动更新
